version: '3.7'

services:
  postgres_certificacao:
    container_name: postgres_certificacao
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pg_certificacao
